# Base URL of the Flask API. Leave empty in local dev so requests go to
# Vite's proxy (configured in vite.config.js → /api → http://localhost:5001).
# For a production build (Pages, Netlify, etc.) point at your hosted server,
# e.g. VITE_API_BASE=https://api.example.com
VITE_API_BASE=
