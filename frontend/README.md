# frontend

React + Vite UI for Claude Decoded.

## Setup

```bash
cd frontend
npm install
npm run dev
```

Opens on `http://localhost:5173`. `/api/*` is proxied to the Flask server on `:5001`.

Run the server (see `../server/README.md`) before clicking **Try it out**.
